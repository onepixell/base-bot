# Ensure consistent behavior across package managers
auto-install-peers=true
strict-peer-dependencies=false

# Hoist workspace dependencies to root
hoist=true

# Save exact versions
save-exact=false
save-prefix=^

# Enable workspace protocol
link-workspace-packages=true
